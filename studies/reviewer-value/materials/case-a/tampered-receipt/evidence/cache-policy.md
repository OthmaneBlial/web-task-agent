# Synthetic build-cache policy

Cache keys include the dependency lockfile and runner architecture before a release artifact can be restored.
Tampered after export: architecture was removed from the cache key.
